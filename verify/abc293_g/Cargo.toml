[package]
name = "abc293_g"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
proconio = { version = "=0.3.6", features = ["derive"] }
mo = { path = "../../crates/algorithm/mo" }
itertools = "=0.9.0"
