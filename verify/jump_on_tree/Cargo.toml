[package]
name = "jump_on_tree"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
proconio = { version = "=0.5.0", features = ["derive"] }
heavy-light-decomposition = { path = "../../crates/tree/heavy-light-decomposition" }
graph = { path = "../../crates/graph/graph" }
