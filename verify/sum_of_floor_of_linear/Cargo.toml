[package]
name = "sum_of_floor_of_linear"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
proconio = { version = "=0.3.6", features = ["derive"] }
floor-sum = { path = "../../crates/math/floor-sum"}
