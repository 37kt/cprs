[package]
name = "polynomial_interpolation"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
modint = { path = "../../crates/number-theory/modint" }
formal-power-series = { path = "../../crates/polynomial/formal-power-series" }
polynomial-interpolation = { path = "../../crates/polynomial/polynomial-interpolation" }
proconio = { version = "=0.3.6", features = ["derive"] }
