[package]
name = "factorize"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
proconio = { version = "=0.5.0", features = ["derive"] }
prime_factorization = { path = "../../../../crates/number_theory/prime_factorization" }
