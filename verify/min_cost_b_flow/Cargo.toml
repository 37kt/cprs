[package]
name = "min_cost_b_flow"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
proconio = { version = "=0.3.6", features = ["derive"] }
min-cost-b-flow = { path = "../../crates/graph/min-cost-b-flow" }
itertools = "=0.9.0"
