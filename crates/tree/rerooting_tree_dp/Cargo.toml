[package]
name = "rerooting_tree_dp"
version = "0.1.0"
edition = "2021"
license = "CC0-1.0"
publish = false

[lib]
name = "rerooting_tree_dp"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
csr_array = { path = "../../data_structure/csr_array" }
graph = { path = "../../graph/graph" }
