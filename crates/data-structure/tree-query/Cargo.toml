[package]
name = "tree-query"
version = "0.1.0"
edition = "2018"
license = "CC0-1.0"
publish = false

[lib]
name = "tree_query"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
heavy-light-decomposition = { path = "../heavy-light-decomposition" }
segment-tree = { path = "../segment-tree" }
graph = { path = "../../graph/graph" }
algebraic = { path = "../../algebraic/algebraic" }
